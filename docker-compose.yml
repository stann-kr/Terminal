services:
  web:
    build: .
    container_name: terminal-web
    ports:
      - "3000:3000"
    volumes:
      - ./web:/app
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
    working_dir: /app
